import 'package:flutter/material.dart';

class NewShopScreen extends StatelessWidget {
  const NewShopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}